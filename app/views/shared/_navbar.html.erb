<nav class="navbar navbar-transparent">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#main-navbar-collapse" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "SUNGWON RYU", root_path, class: "navbar-brand" %> 
    </div>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="main-navbar-collapse">
      <ul class="nav navbar-nav navbar-right">
        <li class="<%= is_active?('/projects') %>"><%= link_to "Pojects", projects_path %></li>
        <li class="<%= is_active?('/blog') %>"><%= link_to "Blog", blog_path %></li>
        <li class="<%= is_active?('/about') %>"><%= link_to "About", about_path %></li>
        <li class="<%= is_active?('/contact') %>"><%= link_to "Contact", contact_path %></li>

        <!-- Conditional links, Verify if a user is signed in -->
        <% if user_signed_in? %>
          <li class="<%= is_active?('/users/edit') %>"><%= link_to "Edit User", edit_user_registration_path %></li>   
          <li><%= link_to "Sign Out", destroy_user_session_path, method: :delete %></li>
        <% else %>

          <!--
          <li class="<%= is_active?('/users/sign_up') %>"><%= link_to "Sign Up", new_user_registration_path %></li>
          <li class="<%= is_active?('/users/sign_in') %>"><%= link_to "Sign In", new_user_session_path %></li>
          -->
        <% end %>

      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>